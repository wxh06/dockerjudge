language: python
cache: pip
services:
  - docker
python:
  - "3.6"
  - "3.6-dev"
  - "3.7"
  - "3.7-dev"
  - "3.8"
  - "3.8-dev"
  - "3.9-dev"
  - "nightly"
before_install:
  - pip install -U pip
  - pip install -U setuptools wheel
install:
  - make pip
  - pip install -U flake8 pylint pytest-cov codecov
  - make docker-pull
script:
  - make lint
  - make pytest
after_script:
  - codecov
